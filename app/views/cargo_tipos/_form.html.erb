<%= form_for(@cargo_tipo) do |f| %>
  <% if @cargo_tipo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cargo_tipo.errors.count, "error") %> prohibited this cargo_tipo from being saved:</h2>

      <ul>
      <% @cargo_tipo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %><br />
    <%= f.text_field :titulo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
